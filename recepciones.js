import { createClient } from '@supabase/supabase-js';
const SUPABASE_URL='https://cgkdztwtodmdteohvuoh.supabase.co';
const SUPABASE_KEY='sb_publishable_sULeDyfJ1l5xfuVhFgXRKA_bsim9qSe';
const supabase=createClient(SUPABASE_URL,SUPABASE_KEY);
let currentUser=null,receipts=[],suppliers=[],products=[];
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const num=v=>Number(v||0),today=()=>new Date().toISOString().slice(0,10);
async function user(){const {data}=await supabase.auth.getUser();currentUser=data?.user||null;return currentUser}
async function load(){
  if(!currentUser)await user();
  if(!currentUser)return;
  const [r,s,p]=await Promise.all([
    supabase.from('qf_receipts').select('*').eq('owner_id',currentUser.id).order('receipt_date',{ascending:false}).order('receipt_time',{ascending:false}),
    supabase.from('qf_suppliers').select('*').eq('owner_id',currentUser.id).order('name'),
    supabase.from('qf_products').select('id,internal_code,name,base_unit,active').eq('owner_id',currentUser.id).eq('active',true).order('name')
  ]);
  if(r.error)throw r.error;if(s.error)throw s.error;if(p.error)throw p.error;
  receipts=r.data||[];suppliers=s.data||[];products=p.data||[];
  if(receipts.length){
    const ids=receipts.map(x=>x.id);
    const {data:items,error:itemError}=await supabase.from('qf_receipt_items').select('*').in('receipt_id',ids).order('created_at');
    if(itemError)throw itemError;
    const byReceipt=new Map();for(const item of(items||[])){const list=byReceipt.get(item.receipt_id)||[];list.push(item);byReceipt.set(item.receipt_id,list)}
    for(const x of receipts)x.items=byReceipt.get(x.id)||[];
  }
}
function addNav(){
  const nav=document.querySelector('.qf-nav, nav');
  if(!nav)return;
  const els=[...nav.querySelectorAll('button,a')];
  let entrada=null;
  for(const el of els){
    const text=el.textContent.trim().toLowerCase();
    if(text==='despachos'){el.textContent='Salidas';continue}
    if(text==='recepciones'||text==='entradas'||el.dataset.qfRecNav==='1'){
      if(!entrada){entrada=el;entrada.textContent='Entradas';entrada.dataset.qfRecNav='1'}
      else if(el!==entrada)el.remove();
    }
  }
  if(entrada)return;
  const inv=els.find(x=>x.textContent.trim().toLowerCase()==='inventario');
  const a=document.createElement('button');a.type='button';a.textContent='Entradas';a.dataset.qfRecNav='1';a.className='qf-nav-btn';
  a.addEventListener('click',e=>{e.preventDefault();window.qfOpenRecepciones?.()});
  if(inv)inv.after(a);else nav.appendChild(a);
}
function getContent(){return document.querySelector('#content,[data-main-content],main.content,.content-area,.main-content')}
function render(){addNav();const target=getContent();if(!target){console.warn('QUIMFLUX Entradas: no se encontró el contenedor de contenido');return}const weight=receipts.reduce((a,r)=>a+num(r.total_weight_kg),0),pending=receipts.filter(r=>r.status==='Pendiente de revisión').length;target.innerHTML=`<main class="qf-rec-page"><div class="qf-rec-titlebar"><div><h1>Entradas</h1><p>Ingreso de materiales, guías de remisión y control de proveedores.</p></div><button class="primary qf-rec-new">+ Nueva entrada</button></div><section class="qf-rec-kpis"><div class="qf-rec-card"><span>Entradas</span><strong>${receipts.length}</strong></div><div class="qf-rec-card"><span>Peso recibido</span><strong>${weight.toLocaleString('es-PE')} kg</strong></div><div class="qf-rec-card"><span>Proveedores</span><strong>${new Set(receipts.map(r=>r.supplier_name).filter(Boolean)).size}</strong></div><div class="qf-rec-card"><span>Pendientes de revisión</span><strong>${pending}</strong></div></section><section class="qf-rec-panel"><div class="qf-rec-panel-head"><h2>Entradas de materiales</h2><span>${receipts.length} registro(s)</span></div>${receipts.length?`<div class="qf-rec-table-wrap"><table class="qf-rec-table"><thead><tr><th>Fecha</th><th>Guía</th><th>Proveedor</th><th>OC</th><th>Peso</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>${receipts.map(r=>`<tr><td>${esc(r.receipt_date)}</td><td><b>${esc([r.guide_series,r.guide_number].filter(Boolean).join('-')||'Sin guía')}</b></td><td>${esc(r.supplier_name||'')}</td><td>${esc(r.purchase_order||'—')}</td><td>${num(r.total_weight_kg).toLocaleString('es-PE')} kg</td><td><span class="qf-rec-status">${esc(r.status)}</span></td><td><button class="secondary qf-rec-edit" data-id="${r.id}">Ver / editar</button> <button class="secondary qf-rec-del" data-id="${r.id}">Eliminar</button></td></tr>`).join('')}</tbody></table></div>`:`<div class="qf-rec-empty"><div class="qf-rec-empty-icon">📥</div><h3>Aún no hay entradas</h3><p>Registra aquí las guías con las que los proveedores ingresan materiales a la planta.</p><button class="primary qf-rec-first">+ Registrar primera entrada</button></div>`}</section></main>`;target.querySelector('.qf-rec-new')?.addEventListener('click',()=>form());target.querySelector('.qf-rec-first')?.addEventListener('click',()=>form());target.querySelectorAll('.qf-rec-edit').forEach(b=>b.onclick=()=>form(b.dataset.id));target.querySelectorAll('.qf-rec-del').forEach(b=>b.onclick=()=>remove(b.dataset.id))}
function form(id=null){const r=id?receipts.find(x=>x.id===id):null,modal=document.createElement('div');modal.className='qf-rec-modal';modal.innerHTML=`<div class="qf-rec-modal-card"><div class="qf-rec-modal-head"><div><h2>${r?'Editar entrada':'Nueva entrada'}</h2><p>Registra el documento, transporte y materiales recibidos.</p></div><button type="button" class="qf-rec-close">×</button></div><form id="qfRecForm"><div class="qf-rec-section"><h3>Documento y proveedor</h3><div class="qf-rec-grid"><label>Fecha<input name="receipt_date" type="date" required value="${esc(r?.receipt_date||today())}"></label><label>Hora<input name="receipt_time" type="time" value="${esc(r?.receipt_time||'')}"></label><label>Tipo<select name="document_type"><option>Guía de Remisión</option><option>Factura</option><option>Otro</option></select></label><label>Serie<input name="guide_series" placeholder="E001" value="${esc(r?.guide_series||'')}"></label><label>Número<input name="guide_number" placeholder="00001234" value="${esc(r?.guide_number||'')}"></label><label>Orden de compra<input name="purchase_order" placeholder="OC-..." value="${esc(r?.purchase_order||'')}"></label><label class="wide">Proveedor<select name="supplier_id" id="qfSupplier"><option value="">— Seleccionar proveedor —</option>${suppliers.map(s=>`<option value="${s.id}" ${r?.supplier_id===s.id?'selected':''}>${esc(s.name)}${s.ruc?' · RUC '+esc(s.ruc):''}</option>`).join('')}</select></label><label>Nombre proveedor<input name="supplier_name" id="qfSupplierName" required value="${esc(r?.supplier_name||'')}"></label><label>RUC<input name="supplier_ruc" id="qfSupplierRuc" value="${esc(r?.supplier_ruc||'')}"></label></div></div><div class="qf-rec-section"><h3>Transporte</h3><div class="qf-rec-grid"><label>Placa<input name="vehicle_plate" value="${esc(r?.vehicle_plate||'')}"></label><label>Conductor<input name="driver_name" value="${esc(r?.driver_name||'')}"></label><label>Licencia<input name="driver_license" value="${esc(r?.driver_license||'')}"></label><label>Origen<input name="origin" value="${esc(r?.origin||'')}"></label></div></div><div class="qf-rec-section"><div class="qf-rec-lines-head"><h3>Materiales recibidos</h3><button type="button" class="secondary" id="qfAddLine">+ Agregar material</button></div><div id="qfLines"></div></div><div class="qf-rec-section"><div class="qf-rec-grid"><label>Estado<select name="status"><option>Registrada</option><option>Pendiente de revisión</option><option>Recibida</option><option>Aprobada</option><option>Rechazada</option></select></label><label class="wide">Observaciones<textarea name="observations" rows="3">${esc(r?.observations||'')}</textarea></label></div></div><div class="qf-rec-form-actions"><button type="button" class="secondary qf-rec-cancel">Cancelar</button><button class="primary" type="submit">${r?'Guardar cambios':'Guardar entrada'}</button></div></form></div>`;document.body.appendChild(modal);modal.querySelector('.qf-rec-close').onclick=()=>modal.remove();modal.querySelector('.qf-rec-cancel').onclick=()=>modal.remove();const supplier=modal.querySelector('#qfSupplier');supplier.onchange=()=>{const s=suppliers.find(x=>x.id===supplier.value);if(s){modal.querySelector('#qfSupplierName').value=s.name||'';modal.querySelector('#qfSupplierRuc').value=s.ruc||''}};const lines=modal.querySelector('#qfLines');const add=item=>{const d=document.createElement('div');d.className='qf-rec-line';d.innerHTML=`<label>Producto<select class="lp"><option value="">— Producto —</option>${products.map(p=>`<option value="${p.id}" ${item?.product_id===p.id?'selected':''}>${esc(p.name)} (${esc(p.internal_code||'')})</option>`).join('')}</select></label><label>Cant. guía<input class="lg" type="number" min="0" step="0.001" value="${item?.quantity_guide??''}"></label><label>Cant. recibida<input class="lr" type="number" min="0" step="0.001" value="${item?.quantity_received??''}"></label><label>Peso kg<input class="lw" type="number" min="0" step="0.001" value="${item?.weight_kg??''}"></label><label>Lote<input class="ll" value="${esc(item?.lot||'')}"></label><label>Estado<select class="lc"><option>Conforme</option><option ${item?.condition==='Con diferencia'?'selected':''}>Con diferencia</option><option ${item?.condition==='Rechazado'?'selected':''}>Rechazado</option></select></label><button type="button" class="qf-line-remove">×</button>`;lines.appendChild(d);d.querySelector('.qf-line-remove').onclick=()=>d.remove()};(r?.items?.length?r.items:[{}]).forEach(add);modal.querySelector('#qfAddLine').onclick=()=>add({});modal.querySelector('#qfRecForm').onsubmit=async e=>{e.preventDefault();await save(id,modal,lines)}}
async function save(id,modal,lines){const f=new FormData(modal.querySelector('form'));const items=[...lines.querySelectorAll('.qf-rec-line')].map(d=>{const p=products.find(x=>x.id===d.querySelector('.lp').value);return{product_id:p?.id||null,codigo:p?.internal_code||null,material:p?.name||null,description:p?.name||'Material',unit:p?.base_unit||null,quantity_guide:num(d.querySelector('.lg').value),quantity_received:num(d.querySelector('.lr').value),weight_kg:num(d.querySelector('.lw').value),lot:d.querySelector('.ll').value.trim(),condition:d.querySelector('.lc').value}}).filter(x=>x.product_id||x.quantity_guide||x.quantity_received||x.weight_kg);if(!f.get('supplier_name')?.trim()){alert('Ingresa el proveedor.');return}if(!items.length){alert('Agrega al menos un material.');return}const payload={owner_id:currentUser.id,receipt_date:f.get('receipt_date'),receipt_time:f.get('receipt_time')||null,document_type:f.get('document_type'),guide_series:f.get('guide_series')?.trim()||null,guide_number:f.get('guide_number')?.trim()||null,supplier_id:f.get('supplier_id')||null,supplier_name:f.get('supplier_name').trim(),supplier_ruc:f.get('supplier_ruc')?.trim()||null,purchase_order:f.get('purchase_order')?.trim()||null,vehicle_plate:f.get('vehicle_plate')?.trim()||null,driver_name:f.get('driver_name')?.trim()||null,driver_license:f.get('driver_license')?.trim()||null,origin:f.get('origin')?.trim()||null,status:f.get('status'),total_quantity:items.reduce((a,x)=>a+num(x.quantity_received),0),total_weight_kg:items.reduce((a,x)=>a+num(x.weight_kg),0),observations:f.get('observations')?.trim()||null,updated_at:new Date().toISOString()};let receiptId=id,err;if(id){({error:err}=await supabase.from('qf_receipts').update(payload).eq('id',id).eq('owner_id',currentUser.id));if(!err)await supabase.from('qf_receipt_items').delete().eq('receipt_id',id)}else{const z=await supabase.from('qf_receipts').insert(payload).select('id').single();err=z.error;receiptId=z.data?.id}if(err){alert('No se pudo guardar: '+err.message);return}const z=await supabase.from('qf_receipt_items').insert(items.map(x=>({...x,receipt_id:receiptId})));if(z.error){alert('Entrada guardada, pero hubo un error con los materiales: '+z.error.message);return}modal.remove();await load();render()}
async function remove(id){if(!confirm('¿Eliminar esta entrada y sus materiales?'))return;const {error}=await supabase.from('qf_receipts').delete().eq('id',id).eq('owner_id',currentUser.id);if(error){alert('No se pudo eliminar: '+error.message);return}await load();render()}
window.qfOpenRecepciones=async()=>{try{await user();if(!currentUser){alert('Inicia sesión para acceder a Entradas.');return}await load();render()}catch(e){console.error('Entradas:',e);alert('No se pudo cargar Entradas: '+(e?.message||e))}};
async function init(){try{await user();if(currentUser){await load();addNav()}}catch(e){console.error('Entradas:',e)}supabase.auth.onAuthStateChange(async(_event,session)=>{currentUser=session?.user||null;if(currentUser){try{await load();addNav()}catch(e){console.error('Entradas:',e)}}else document.querySelector('[data-qf-rec-nav]')?.remove()});new MutationObserver(addNav).observe(document.body,{childList:true,subtree:true})}
init();