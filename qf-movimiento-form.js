import { createClient } from '@supabase/supabase-js';

const supabase=createClient('https://cgkdztwtodmdteohvuoh.supabase.co','sb_publishable_sULeDyfJ1l5xfuVhFgXRKA_bsim9qSe');
const DOCS=['Guía de Remisión','Factura','Boleta','Nota de Crédito','Nota de Débito','Otro'];
const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
const num=v=>Number.isFinite(Number(v))?Number(v):0;
const today=()=>new Date().toISOString().slice(0,10);

function installStyle(){
  if(document.getElementById('qf-movimiento-form-style'))return;
  const s=document.createElement('style');s.id='qf-movimiento-form-style';s.textContent=`
    .qf-mf-row{display:flex;gap:8px;align-items:end}.qf-mf-row>label{flex:1;min-width:0}
    .qf-mf-new{height:42px;white-space:nowrap;padding:0 12px!important}
    .qf-mf-panel{margin-top:10px;padding:12px;border:1px solid #33423d;border-radius:10px;background:#0d1312}
    .qf-mf-panel-title{font-weight:700;margin-bottom:10px}.qf-mf-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}
    .qf-mf-panel label{display:flex;flex-direction:column;gap:5px;color:#9db0ba;font-size:13px}.qf-mf-panel input,.qf-mf-panel select{box-sizing:border-box;width:100%;background:#0b1110;color:#f3f7f8;border:1px solid #33423d;border-radius:8px;padding:9px 10px;font:inherit}
    .qf-mf-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:10px}.qf-mf-msg{font-size:13px;color:#ff9c9c;margin-top:7px}
    @media(max-width:760px){.qf-mf-row{flex-direction:column;align-items:stretch}.qf-mf-new{width:100%}.qf-mf-grid{grid-template-columns:1fr}}
  `;document.head.appendChild(s);
}

async function user(){const {data,error}=await supabase.auth.getUser();if(error)throw error;if(!data?.user)throw Error('La sesión no está disponible.');return data.user}
async function masters(){const u=await user();const [c,s,v,d,p,o,oc]=await Promise.all([
  supabase.from('qf_customers').select('id,business_name,ruc').eq('owner_id',u.id).eq('active',true).order('business_name'),
  supabase.from('qf_suppliers').select('id,name,ruc').eq('owner_id',u.id).order('name'),
  supabase.from('qf_vehicles').select('id,plate').eq('owner_id',u.id).eq('active',true).order('plate'),
  supabase.from('qf_drivers').select('id,full_name,license_number').eq('owner_id',u.id).eq('active',true).order('full_name'),
  supabase.from('qf_products').select('id,internal_code,name,base_unit').eq('owner_id',u.id).eq('active',true).order('name'),
  supabase.from('qf_master_options').select('option_type,value').eq('owner_id',u.id).eq('active',true).order('value'),
  supabase.from('qf_purchase_orders').select('id,oc_number,operating_unit,status,customer_id').eq('owner_id',u.id).order('oc_number')
]);
  for(const r of [c,s,v,d,p,o,oc])if(r.error)throw r.error;
  return {u,customers:c.data||[],suppliers:s.data||[],vehicles:v.data||[],drivers:d.data||[],products:p.data||[],locations:o.data||[],orders:oc.data||[]};
}
function options(items,selected,placeholder,text){return `<option value="">${esc(placeholder)}</option>`+items.map(x=>`<option value="${esc(text(x).value)}" ${String(text(x).value)===String(selected??'')?'selected':''}>${esc(text(x).label)}</option>`).join('')}
function fieldWithButton(label,control,buttonType,buttonText){return `<div class="qf-mf-row"><label>${label}${control}</label><button type="button" class="secondary qf-mf-new" data-qf-new="${buttonType}">+ ${buttonText}</button></div>`}

function addDestinationIfMissing(grid,value=''){if(grid.querySelector('[name="destination"]'))return;grid.insertAdjacentHTML('beforeend',`<label>Destino<select name="destination"><option value="">— Seleccionar destino —</option></select></label>`);grid.querySelector('[name="destination"]').dataset.initial=value||''}

async function openMasterPanel(modal,type,button,refresh){
  const old=modal.querySelector(`[data-qf-panel="${type}"]`);if(old){old.remove();return}
  const cfg={
    customer:['Nuevo cliente','Razón social / nombre','RUC'],supplier:['Nuevo proveedor','Razón social / nombre','RUC'],vehicle:['Nuevo transporte','Placa',''],driver:['Nuevo conductor','Nombre completo','Licencia'],origin:['Nuevo origen','Origen',''],destination:['Nuevo destino','Destino',''],product:['Nuevo producto','Nombre del producto','Código interno'],order:['Nueva O/C','N.º de O/C','Unidad / referencia']
  }[type];
  const p=document.createElement('div');p.className='qf-mf-panel';p.dataset.qfPanel=type;
  let body='';
  if(type==='product')body='<label>Nombre del producto<input class="a" required></label><label>Código interno<input class="b" required></label><label>Unidad<select class="c"><option>kg</option><option>unidad</option><option>saco</option><option>caja</option><option>litro</option><option>otro</option></select></label>';
  else if(type==='driver')body='<label>Nombre completo<input class="a" required></label><label>Licencia<input class="b" required></label>';
  else if(type==='order')body='<label>N.º de O/C<input class="a" required></label><label>Unidad / referencia<input class="b"></label><label>Estado<select class="c"><option value="pendiente">Pendiente</option><option value="parcial">Parcial</option><option value="atendida">Atendida</option><option value="cancelada">Cancelada</option></select></label>';
  else if(type==='customer'||type==='supplier')body=`<label>Razón social / nombre<input class="a" required></label><label>RUC<input class="b"></label>`;
  else body=`<label>${cfg[1]}<input class="a" required></label>`;
  p.innerHTML=`<div class="qf-mf-panel-title">${cfg[0]}</div><div class="qf-mf-grid">${body}</div><div class="qf-mf-actions"><button type="button" class="secondary qf-mf-cancel">Cancelar</button><button type="button" class="primary qf-mf-save">Guardar</button></div><div class="qf-mf-msg"></div>`;
  const section=button.closest('.qf-rec-section')||modal.querySelector('.qf-rec-section');section.appendChild(p);
  p.querySelector('.qf-mf-cancel').onclick=()=>p.remove();
  p.querySelector('.qf-mf-save').onclick=async()=>{const msg=p.querySelector('.qf-mf-msg');const a=p.querySelector('.a')?.value.trim();const b=p.querySelector('.b')?.value.trim();try{const u=await user();let selected='';
    if(!a){msg.textContent='Completa el dato requerido.';return}
    if(type==='customer'){const q=await supabase.from('qf_customers').select('id').eq('owner_id',u.id).ilike('business_name',a).limit(1);if(q.error)throw q.error;const row=q.data?.[0]|| (await supabase.from('qf_customers').insert({owner_id:u.id,business_name:a,ruc:b||null,active:true}).select('id').single()).data;if(!row)throw Error('No se pudo registrar el cliente.');selected=row.id}
    if(type==='supplier'){const q=await supabase.from('qf_suppliers').select('id').eq('owner_id',u.id).ilike('name',a).limit(1);if(q.error)throw q.error;const row=q.data?.[0]|| (await supabase.from('qf_suppliers').insert({owner_id:u.id,name:a,ruc:b||null}).select('id').single()).data;if(!row)throw Error('No se pudo registrar el proveedor.');selected=row.id}
    if(type==='vehicle'){const q=await supabase.from('qf_vehicles').select('id,plate').eq('owner_id',u.id).ilike('plate',a).limit(1);if(q.error)throw q.error;const row=q.data?.[0]|| (await supabase.from('qf_vehicles').insert({owner_id:u.id,plate:a,active:true}).select('id,plate').single()).data;if(!row)throw Error('No se pudo registrar el transporte.');selected=row.plate}
    if(type==='driver'){if(!b)throw Error('Ingresa la licencia.');const q=await supabase.from('qf_drivers').select('id,full_name,license_number').eq('owner_id',u.id).ilike('full_name',a).limit(1);if(q.error)throw q.error;const row=q.data?.[0]|| (await supabase.from('qf_drivers').insert({owner_id:u.id,full_name:a,license_number:b,active:true}).select('id,full_name,license_number').single()).data;if(!row)throw Error('No se pudo registrar el conductor.');selected=row.full_name;const lic=modal.querySelector('[name="driver_license"]');if(lic)lic.value=row.license_number||''}
    if(type==='origin'||type==='destination'){const ot=type==='origin'?'origen':'destino';const q=await supabase.from('qf_master_options').select('id').eq('owner_id',u.id).eq('option_type',ot).ilike('value',a).limit(1);if(q.error)throw q.error;if(!q.data?.length){const x=await supabase.from('qf_master_options').insert({owner_id:u.id,option_type:ot,value:a,active:true}).select('id').single();if(x.error)throw x.error}selected=a}
    if(type==='product'){if(!b)throw Error('Ingresa el código interno.');const unit=p.querySelector('.c').value;const x=await supabase.from('qf_products').insert({owner_id:u.id,internal_code:b,name:a,base_unit:unit,active:true}).select('id').single();if(x.error)throw x.error;selected=x.data.id}
    if(type==='order'){let customerId=modal.querySelector('[name="customer_id"]')?.value||null;if(!customerId){const supplierId=modal.querySelector('[name="supplier_id"]')?.value;if(supplierId){const z=await supabase.from('qf_suppliers').select('name,ruc').eq('id',supplierId).maybeSingle();if(z.error)throw z.error;if(z.data){const c=await supabase.from('qf_customers').select('id').eq('owner_id',u.id).ilike('business_name',z.data.name).limit(1);if(c.error)throw c.error;customerId=c.data?.[0]?.id;if(!customerId){const cr=await supabase.from('qf_customers').insert({owner_id:u.id,business_name:z.data.name,ruc:z.data.ruc||null,active:true}).select('id').single();if(cr.error)throw cr.error;customerId=cr.data.id}}}}if(!customerId)throw Error('Selecciona primero cliente o proveedor para asociar la O/C.');const x=await supabase.from('qf_purchase_orders').insert({owner_id:u.id,customer_id:customerId,oc_number:a,operating_unit:b||null,status:p.querySelector('.c').value}).select('id,oc_number').single();if(x.error)throw x.error;selected=x.data.id}
    p.remove();await refresh(selected,type);refresh();
  }catch(e){msg.textContent='No se pudo guardar: '+(e?.message||e)}};
}

export async function openMovimientoForm({mode,record=null,onSaved}){
  installStyle();
  const m=await masters();
  const isEntry=mode==='entrada';
  const title=isEntry?(record?'Editar entrada':'Nueva entrada'):(record?'Editar salida':'Nueva salida');
  const thirdLabel=isEntry?'Proveedor':'Cliente';
  const selectedThird=isEntry?record?.supplier_id:record?.customer_id;
  const modal=document.createElement('div');modal.className='qf-rec-modal';
  modal.innerHTML=`<div class="qf-rec-modal-card"><div class="qf-rec-modal-head"><div><h2>${title}</h2><p>Registra documento, ${thirdLabel.toLowerCase()}, transporte y materiales.</p></div><button type="button" class="qf-rec-close">×</button></div><form class="qf-mf-form">
    <div class="qf-rec-section"><h3>Documento y ${thirdLabel.toLowerCase()}</h3><div class="qf-rec-grid">
      <label>Fecha<input name="date" type="date" required value="${esc(record?.[isEntry?'receipt_date':'guide_date']||today())}"></label>
      <label>Hora<input name="time" type="time" value="${esc(String(record?.[isEntry?'receipt_time':'guide_time']||'').slice(0,5))}"></label>
      <label>Tipo de documento<select name="document_type">${DOCS.map(v=>`<option value="${esc(v)}" ${v===(record?.document_type||'Guía de Remisión')?'selected':''}>${esc(v)}</option>`).join('')}</select></label>
      <label>Serie<input name="series" value="${esc(record?.guide_series||'')}"></label>
      <label>Número<input name="number" required value="${esc(record?.guide_number||'')}"></label>
      <div>${isEntry?fieldWithButton('Orden de compra',`<select name="purchase_order"><option value="">— Seleccionar O/C —</option>${options(m.orders,record?.purchase_order,'— Seleccionar O/C —',x=>({value:x.oc_number,label:`${x.oc_number}${x.operating_unit?' · '+x.operating_unit:''}`}))}</select>`:fieldWithButton('Orden de compra',`<select name="purchase_order_id"><option value="">— Seleccionar O/C —</option>${options(m.orders,record?.purchase_order_id,'— Seleccionar O/C —',x=>({value:x.id,label:`${x.oc_number}${x.operating_unit?' · '+x.operating_unit:''}`}))}</select>`,'order','Nueva O/C')}</div>
      <div class="wide">${isEntry?fieldWithButton('Proveedor',`<select name="supplier_id"><option value="">— Seleccionar proveedor —</option>${options(m.suppliers,selectedThird,'— Seleccionar proveedor —',x=>({value:x.id,label:`${x.name}${x.ruc?' · RUC '+x.ruc:''}`}))}</select>`,'supplier','Nuevo proveedor'):fieldWithButton('Cliente',`<select name="customer_id"><option value="">— Seleccionar cliente —</option>${options(m.customers,selectedThird,'— Seleccionar cliente —',x=>({value:x.id,label:`${x.business_name}${x.ruc?' · RUC '+x.ruc:''}`}))}</select>`,'customer','Nuevo cliente')}</div>
      <label>${thirdLabel} seleccionado<input name="third_name" readonly value="${esc(isEntry?record?.supplier_name||'':(m.customers.find(x=>x.id===selectedThird)?.business_name||''))}"></label>
      <label>RUC<input name="third_ruc" readonly value="${esc(isEntry?record?.supplier_ruc||'':(m.customers.find(x=>x.id===selectedThird)?.ruc||''))}"></label>
    </div></div>
    <div class="qf-rec-section"><h3>Transporte</h3><div class="qf-rec-grid">
      <div>${fieldWithButton('Placa',`<select name="vehicle"><option value="">— Seleccionar vehículo —</option>${options(m.vehicles,record?.vehicle_id||record?.vehicle_plate,'— Seleccionar vehículo —',x=>({value:isEntry?x.plate:x.id,label:x.plate}))}</select>`,'vehicle','Nuevo transporte')}</div>
      <div>${fieldWithButton('Conductor',`<select name="driver"><option value="">— Seleccionar conductor —</option>${options(m.drivers,record?.driver_id||record?.driver_name,'— Seleccionar conductor —',x=>({value:isEntry?x.full_name:x.id,label:x.full_name}))}</select>`,'driver','Nuevo conductor')}</div>
      <label>Licencia<input name="driver_license" readonly value="${esc(record?.driver_license||(m.drivers.find(x=>x.id===record?.driver_id)?.license_number)||'')}"></label>
      <div>${fieldWithButton('Origen',`<select name="origin"><option value="">— Seleccionar origen —</option>${options(m.locations.filter(x=>x.option_type==='origen'),record?.origin,'— Seleccionar origen —',x=>({value:x.value,label:x.value}))}</select>`,'origin','Nuevo origen')}</div>
      <div>${fieldWithButton('Destino',`<select name="destination"><option value="">— Seleccionar destino —</option>${options(m.locations.filter(x=>x.option_type==='destino'),record?.destination,'— Seleccionar destino —',x=>({value:x.value,label:x.value}))}</select>`,'destination','Nuevo destino')}</div>
    </div></div>
    <div class="qf-rec-section"><div class="qf-rec-lines-head"><h3>${isEntry?'Materiales recibidos':'Materiales despachados'}</h3><button type="button" class="secondary" data-add-line>+ Agregar material</button></div><div class="qf-mf-product-add"></div><div data-lines></div></div>
    <div class="qf-rec-section"><div class="qf-rec-grid"><label>Estado<select name="status">${(isEntry?['Registrada','Pendiente de revisión','Recibida','Aprobada','Rechazada']:['registrado','preparando','despachado','entregado','anulado']).map(v=>`<option value="${esc(v)}" ${v===record?.status?'selected':''}>${esc(v)}</option>`).join('')}</select></label><label class="wide">Observaciones<textarea name="observations" rows="3">${esc(record?.observations||'')}</textarea></label></div></div>
    <div class="qf-rec-form-actions"><button type="button" class="secondary qf-rec-cancel">Cancelar</button><button type="submit" class="primary">${record?'Guardar cambios':isEntry?'Guardar entrada':'Guardar salida'}</button></div>
  </form></div>`;
  document.body.appendChild(modal);
  const close=()=>modal.remove();modal.querySelector('.qf-rec-close').onclick=close;modal.querySelector('.qf-rec-cancel').onclick=close;
  const form=modal.querySelector('form');
  const third=modal.querySelector(`[name="${isEntry?'supplier_id':'customer_id'}"]`);
  const updateThird=()=>{const row=(isEntry?m.suppliers:m.customers).find(x=>x.id===third.value);modal.querySelector('[name="third_name"]').value=row?(isEntry?row.name:row.business_name):'';modal.querySelector('[name="third_ruc"]').value=row?.ruc||''};third.onchange=updateThird;
  const driver=modal.querySelector('[name="driver"]');driver.onchange=()=>{const row=m.drivers.find(x=>x.id===driver.value||x.full_name===driver.value);modal.querySelector('[name="driver_license"]').value=row?.license_number||''};
  const lines=modal.querySelector('[data-lines]');
  const addLine=item=>{const d=document.createElement('div');d.className='qf-rec-line';const pOpts=options(m.products,item?.product_id,'— Seleccionar producto —',x=>({value:x.id,label:`${x.name}${x.internal_code?' · '+x.internal_code:''}`}));d.innerHTML=isEntry?`<label>Producto<select class="lp">${pOpts}</select></label><label>Cant. guía<input class="lg" type="number" min="0" step="0.001" value="${esc(item?.quantity_guide??'')}"></label><label>Cant. recibida<input class="lr" type="number" min="0" step="0.001" value="${esc(item?.quantity_received??'')}"></label><label>Peso kg<input class="lw" type="number" min="0" step="0.001" value="${esc(item?.weight_kg??'')}"></label><label>Lote<input class="ll" value="${esc(item?.lot||'')}"></label><label>Estado<select class="lc"><option ${item?.condition==='Conforme'||!item?'selected':''}>Conforme</option><option ${item?.condition==='Con diferencia'?'selected':''}>Con diferencia</option><option ${item?.condition==='Rechazado'?'selected':''}>Rechazado</option></select></label><button type="button" class="qf-line-remove">×</button>`:`<label>Producto<select class="lp">${pOpts}</select></label><label>Cantidad<input class="lq" type="number" min="0" step="0.001" value="${esc(item?.quantity??'')}"></label><label>Peso kg<input class="lw" type="number" min="0" step="0.001" value="${esc(item?.weight_kg??'')}"></label><label>Lote<input class="ll" value="${esc(item?.lot||'')}"></label><button type="button" class="qf-line-remove">×</button>`;lines.appendChild(d);d.querySelector('.qf-line-remove').onclick=()=>{if(lines.children.length>1)d.remove()};
    const ps=d.querySelector('.lp');const add=document.createElement('button');add.type='button';add.className='secondary qf-mf-new';add.textContent='+ Nuevo producto';add.style.marginTop='22px';add.onclick=()=>openMasterPanel(modal,'product',add,async selected=>{const mm=await masters();ps.innerHTML=options(mm.products,selected,'— Seleccionar producto —',x=>({value:x.id,label:`${x.name}${x.internal_code?' · '+x.internal_code:''}`}));ps.value=selected});ps.parentElement?.appendChild(add)};
  (record?.items?.length?record.items:[{}]).forEach(addLine);modal.querySelector('[data-add-line]').onclick=()=>addLine({});
  const refresh=async(selected,type)=>{const mm=await masters();if(type==='customer'||type==='supplier'){third.innerHTML=options(isEntry?mm.suppliers:mm.customers,selected,isEntry?'— Seleccionar proveedor —':'— Seleccionar cliente —',x=>({value:x.id,label:isEntry?`${x.name}${x.ruc?' · RUC '+x.ruc:''}`:`${x.business_name}${x.ruc?' · RUC '+x.ruc:''}`}));third.value=selected;updateThird()}else if(type==='vehicle'){const el=form.querySelector('[name="vehicle"]');el.innerHTML=options(mm.vehicles,isEntry?selected:el.value,'— Seleccionar vehículo —',x=>({value:isEntry?x.plate:x.id,label:x.plate}));if(selected)el.value=selected}else if(type==='driver'){const el=form.querySelector('[name="driver"]');el.innerHTML=options(mm.drivers,isEntry?selected:el.value,'— Seleccionar conductor —',x=>({value:isEntry?x.full_name:x.id,label:x.full_name}));if(selected)el.value=selected;driver.onchange()}else if(type==='origin'||type==='destination'){const el=form.querySelector(`[name="${type}"]`);el.innerHTML=options(mm.locations.filter(x=>x.option_type===(type==='origin'?'origen':'destino')),selected,`— Seleccionar ${type} —`,x=>({value:x.value,label:x.value}));if(selected)el.value=selected}else if(type==='order'){const el=form.querySelector(`[name="${isEntry?'purchase_order':'purchase_order_id'}"]`);el.innerHTML=options(mm.orders,isEntry?selected:el.value,'— Seleccionar O/C —',x=>({value:isEntry?x.oc_number:x.id,label:`${x.oc_number}${x.operating_unit?' · '+x.operating_unit:''}`}));if(selected)el.value=isEntry?mm.orders.find(x=>x.id===selected)?.oc_number||selected:selected}};
  modal.querySelectorAll('[data-qf-new]').forEach(b=>b.onclick=()=>openMasterPanel(modal,b.dataset.qfNew,b,refresh));
  form.onsubmit=async e=>{e.preventDefault();try{const u=await user();const f=new FormData(form);const thirdName=String(f.get('third_name')||'').trim();if(!thirdName){alert(`Selecciona un ${thirdLabel.toLowerCase()}.`);return}
      if(isEntry){const items=[...lines.children].map(d=>{const p=m.products.find(x=>x.id===d.querySelector('.lp').value);return{product_id:p?.id||null,codigo:p?.internal_code||null,material:p?.name||null,description:p?.name||'Material',unit:p?.base_unit||null,quantity_guide:num(d.querySelector('.lg').value),quantity_received:num(d.querySelector('.lr').value),weight_kg:num(d.querySelector('.lw').value),lot:d.querySelector('.ll').value.trim()||null,condition:d.querySelector('.lc').value}}).filter(x=>x.product_id||x.quantity_guide||x.quantity_received||x.weight_kg);if(!items.length)throw Error('Agrega al menos un material.');const payload={owner_id:u.id,receipt_date:f.get('date'),receipt_time:f.get('time')||null,document_type:f.get('document_type'),guide_series:String(f.get('series')||'').trim()||null,guide_number:String(f.get('number')||'').trim()||null,supplier_id:f.get('supplier_id')||null,supplier_name:thirdName,supplier_ruc:String(f.get('third_ruc')||'').trim()||null,purchase_order:String(f.get('purchase_order')||'').trim()||null,vehicle_plate:String(f.get('vehicle')||'').trim()||null,driver_name:String(f.get('driver')||'').trim()||null,driver_license:String(f.get('driver_license')||'').trim()||null,origin:String(f.get('origin')||'').trim()||null,destination:String(f.get('destination')||'').trim()||null,status:f.get('status'),total_quantity:items.reduce((a,x)=>a+num(x.quantity_received),0),total_weight_kg:items.reduce((a,x)=>a+num(x.weight_kg),0),observations:String(f.get('observations')||'').trim()||null,updated_at:new Date().toISOString()};let id=record?.id;if(id){const r=await supabase.from('qf_receipts').update(payload).eq('id',id).eq('owner_id',u.id);if(r.error)throw r.error;const d=await supabase.from('qf_receipt_items').delete().eq('receipt_id',id);if(d.error)throw d.error}else{const r=await supabase.from('qf_receipts').insert(payload).select('id').single();if(r.error)throw r.error;id=r.data.id}const r2=await supabase.from('qf_receipt_items').insert(items.map(x=>({...x,receipt_id:id})));if(r2.error)throw r2.error}
      else {const items=[...lines.children].map(d=>{const p=m.products.find(x=>x.id===d.querySelector('.lp').value);return{product_id:p?.id||null,source_product_code:p?.internal_code||null,description_source:p?.name||'Material',quantity:num(d.querySelector('.lq').value),unit:p?.base_unit||'kg',package_type:null,package_count:null,weight_per_package_kg:null,lot:d.querySelector('.ll').value.trim()||null,condition:'Conforme'}}).filter(x=>x.product_id&&x.quantity>0);if(!items.length)throw Error('Agrega al menos un material con cantidad mayor que cero.');const s=String(f.get('series')||'').trim(),n=String(f.get('number')||'').trim();if(!n)throw Error('Ingresa el número de documento.');const payload={owner_id:u.id,guide_number:s?`${s}-${n}`:n,guide_date:f.get('date'),guide_time:f.get('time')||null,document_type:f.get('document_type'),reason:f.get('reason')||'Venta',customer_id:f.get('customer_id'),purchase_order_id:f.get('purchase_order_id')||null,origin:String(f.get('origin')||'').trim()||null,destination:String(f.get('destination')||'').trim()||null,gross_weight_kg:f.get('gross_weight_kg')===''?null:num(f.get('gross_weight_kg')),vehicle_id:f.get('vehicle')||null,driver_id:f.get('driver')||null,status:f.get('status'),observations:String(f.get('observations')||'').trim()||null};let id=record?.id;if(id){const r=await supabase.from('qf_shipments').update(payload).eq('id',id).eq('owner_id',u.id);if(r.error)throw r.error;const d=await supabase.from('qf_shipment_items').delete().eq('shipment_id',id).eq('owner_id',u.id);if(d.error)throw d.error}else{const r=await supabase.from('qf_shipments').insert(payload).select('id').single();if(r.error)throw r.error;id=r.data.id}const r2=await supabase.from('qf_shipment_items').insert(items.map((x,i)=>({...x,shipment_id:id,owner_id:u.id,line_no:i+1})));if(r2.error)throw r2.error}
      modal.remove();await onSaved?.();
    }catch(e){alert('No se pudo guardar: '+(e?.message||e))}};
}
